<div class="banner banner-small" style="background-image: linear-gradient(-225deg, rgba(0,0,0,0.1) 0%, rgba(0,0,0,0.4) 50%), url('<%= cl_image_path @instrument.photo, height: 300, width: 400, crop: :fill %>');">
  <div class="banner-content">
    <h1><%= @instrument.name %></h1>
    <p>Available for rent in <%= @instrument.location %></p>
  </div>
</div>

<br>

<div class="container">
  <div class="description-and-map">
    <div class="row">
<<<<<<< HEAD
      <%= link_to 'Message owner', conversations_path(sender_id: current_user.id, receiver_id: @instrument.user.id), method: :post, class: 'btn btn-primary'%>
      <%= link_to 'Book this instrument', bookings_path(user: current_user.id, instrument: @instrument.id), method: :post, class: 'btn btn-primary'%>
=======

      <% if current_user.nil? %>
        <%= link_to t(".sign_in", default: "Login to book"), new_user_session_path, class: "btn btn-primary" %>
      <% else %>
        <%= link_to 'Message owner', conversations_path(sender_id: current_user.id, receiver_id: @instrument.user.id), method: :post, class: 'btn btn-primary'%>
        <%= link_to 'Book this instrument', bookings_path(user_id: current_user.id, instrument_id: @instrument.id), method: :post, class: 'btn btn-primary'%>
      <% end %>
      
>>>>>>> d14932122d960473aeac8d49abbc126f30d1d9e9
      <% if policy(@instrument).edit? %>
        <%= link_to "Edit", edit_instrument_path(@instrument), class: 'btn btn-warning' %>
      <% end %>

      <% if policy(@instrument).destroy? %>
        <%= link_to "Delete", instrument_path(@instrument), method: :delete, class: 'btn btn-danger' %>
      <% end %>

      <%= link_to "Back to all instruments", instruments_path, class: 'btn btn-default' %>
    </div>
    
    <br>

    <div class="row">
      <div class="col-sm-6" style="height: 300px; background-image: url('<%= image_url "https://maps.googleapis.com/maps/api/staticmap?center=#{@instrument.location.gsub(' ', '+')}&zoom=13&size=600x300" %>');"></div>
      <div class="col-sm-6">
        <h3>Description</h3>
        <p><%= @instrument.description %></p>
      </div>
    </div>
    <div class="row">
      <div class="col-sm-3 col-sm-offset-6">
        <h3>Price</h3>
        <p>€<%= @instrument.price %></p>
      </div>
      <div class="col-sm-3">
        <h3>Owned by</h3>
        <p><%= @instrument.user.email %></p>
      </div>
    </div>
  </div>
</div>