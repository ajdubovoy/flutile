<div class="container">
  <div class="row">
    <div class="col-sm-12">
      <ul class="list-group">
        <% @conversations.each do |conversation| %>
        <li class="list-group-item" data-conversation-id="<%= conversation.id %>">
          <% if conversation.receiver.name.nil? %>
            <%= link_to conversation.receiver.email, conversation_messages_path(conversation) %>: <%= Booking.find(conversation.booking_id).instrument.name %>
          <% else %>
            <%= link_to conversation.receiver.name, conversation_messages_path(conversation) %>: <%= Booking.find(conversation.booking_id).instrument.name %>
          <% end %>
          <% if !conversation.unread_message_count(current_user).zero? %>
          <span class="badge"><%= conversation.unread_message_count(current_user) %></span>
          <% end %>
        </li>
        <% end %>
      </ul>
    </div>
  </div>
</div>